import { Routes } from '@angular/router';
import { ImuladorComponent } from './components/imulador/imulador.component';

export const routes: Routes = [
    { path:'', component: ImuladorComponent}
];
